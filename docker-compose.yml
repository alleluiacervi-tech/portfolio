services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: spaceportfolio:latest
    container_name: spaceportfolio-web
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      HOSTNAME: 0.0.0.0
      PORT: 3000
